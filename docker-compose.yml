version: '3.8'
services:
  backend:
  build:
    context: ./Backend
  ports:
    - "3000:3000"
  networks:
    - mern-network
  environment:
    - MONGO_URL=mongodb+srv://azsafrah:Safrah710.@cluster0.8qfji0b.mongodb.net/?retryWrites=true&w=majority&appName=Cluster0
  dns:
    - 8.8.8.8
    - 8.8.4.4

  frontend:
    build:
      context: ./frontend
    ports:
      - "5173:5173"
    networks:
      - mern-network
    depends_on:
      - backend

networks:
  mern-network:
    driver: bridge
